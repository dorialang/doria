function grade(int $score): string
{
    return match (true) {
        $score >= 90 => "A",
        $score >= 80 => "B",
        $score >= 70 => "C",
        default => "F",
    };
}

function main(): void
{
    echo "{grade(95)} {grade(84)} {grade(73)} {grade(20)}\n";
}
