function main(): int
{
    int8 $value = -85;
    int8 $divisor = 2;
    int8 $quotient = $value / $divisor;
    int8 $remainder = $value % $divisor;

    if ($quotient == -42 and $remainder == -1) {
        return 42;
    }

    return 0;
}
