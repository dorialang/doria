function main(): void throws Doria\Std\Io\IoError
{
    let writable $index = 0;
    given {
        int $limit = 2;
        true;
    } while ($index < $limit) {
        $index++;
    } finally {
        echo "cleanup {$index}/{$limit}\n";
    }
}
