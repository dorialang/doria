class Label
{
    function __construct(string $text)
    {
    }
}

function choose(bool $primary): Label
{
    return when ($primary): Label {
        return new Label("primary");
    } else {
        return new Label("fallback");
    };
}

function main(): void throws Doria\Std\Io\IoError
{
    let $label = choose(true);
    echo "owned\n";
}
