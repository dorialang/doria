class Token
{
    function __construct(string $name)
    {
    }

    function __destruct()
    {
        try {
            echo "drop {$this->name}\n";
        } catch (Doria\Std\Io\IoError) {
        }
    }
}

function main(): void
{
    {
        let $outer = new Token("outer");

        {
            let $inner = new Token("inner");
            echo "inside\n";
        }

        echo "after inner\n";
    }

    echo "done\n";
}
