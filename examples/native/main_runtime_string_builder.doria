function repeat(string $piece, int $count): string
{
    let writable $result = "";

    for (let writable $index = 0; $index < $count; $index++) {
        $result = $result . $piece;
    }

    return $result;
}

function main(): void throws Doria\Std\Io\IoError
{
    echo repeat("Doria", 2);
}
