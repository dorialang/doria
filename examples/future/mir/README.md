# Future MIR Fixtures

These files are future acceptance fixtures for MIR, runtime, and native backend stages.

They are not current native smoke examples.

Do not include them in current native smoke tests.

They become active only after MIR + interpreter oracle and the relevant bool/string runtime support land.

They were salvaged from feature/stage-11-native-primitive-helper-signatures after Stage 11 was redefined as MIR + interpreter oracle.
