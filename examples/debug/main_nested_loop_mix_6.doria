function main(): int
{
    let writable $count = 0;

    foreach (0..<3 as $outer) {
        for (let writable $inner = 0; $inner < 2; $inner++) {
            $count++;
        }
    }

    return $count;
}
