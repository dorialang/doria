# Doria End-to-End Plan Adoption

> Documentation role: historical note.
> This file is preserved for context only. It is not an active roadmap or source of truth. Use `docs/doria-end-to-end-plan.md` for current execution planning.

The updated Doria End-to-End Development Plan is now the master execution plan for future work.

It supersedes older planning wording where the plan explicitly resolves a future-work fork.

Already-implemented behavior remains governed by existing compiler behavior and accepted decisions until a later stage migrates it.

The next implementation stage is Stage 11: MIR + interpreter oracle.

The pre-plan branch feature/stage-11-native-primitive-helper-signatures is not adopted as Stage 11. Useful tests/examples may be salvaged as future fixtures, but its native smoke implementation must not be merged.

This note is not an accepted language decision.
